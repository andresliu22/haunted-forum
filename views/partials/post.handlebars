<a href="/posts/{{id}}" class="post-card mb-2" style="text-decoration: none;" style="height: 175px">
<div class="card w-100" style="height: 160px overflow: hidden;">

  <div class="card-body d-flex w-100">
    <div>
      <img class="float-left" src="{{image_link}}" style="max-width:60px">
    </div>
    <div>
      <h5 class="card-title" style="color: black;">{{title}}</h5>
      <h6>Posted on {{format_date creation_date}} at {{format_time creation_date}}</h6>
      <h6 class="card-subtitle mb-2 text-muted">by <b style="color: black;">{{user.username}}</b> in <b style="color:black">{{location.name}}</b></h6>
      <p class="card-text">{{trimText body}}</p>
      <div class="w-100 d-flex justify-content-evenly">
        <p>Comments: {{comments.length}}</p>
        <p>Upvotes: {{upVotes}}</p>
      </div>
    </div>

  </div>
</div>
</a>